# O total de fronteiras que teríamos que estimar é 
    # 1 quinquilhão, 152 quatrilhões, 921 trilhões, 504 bilhões, 606 milhões, 847 mil
# A solução extensiva é absolutamente impraticável

# Opçao 1:
    # sampling; ao invés de estabelecermos o melhor portfolio em cada universo,
    # definimos apenas o portfolio médio e o stdDev dele.
        # com o dado de stdDev, sabemos qual a probabilidade de o pf convergir
        # já aqui, pois sabemos qual é a chance de um pf solto ser
        # x% melhor do que o médio

# Opção 2:
    # Rodar até atingir um "nível" de convergência, sem chegar ao 60, com um timeout. 
        # Como ja em um universo de 10 ações nós superamos a marca do bilhão,
        # talvez só não funcione e bata no timeout muito antes de se aprox. 
        # de convergir

# Opção 3:
    # Opção 3, com imposição de pesos constantes
    # Talvez isso não baste para corregir o defeito da 2

# Opção 4:
    # Opção 1 e 3;
    # para cada nível rodar até convergir internamente, com pesos fixos, e só
    # rodar níveis até convergir, não até o 60 necessariamente

# Opção 5:
    # Estima, para o portfolio de mercado, StdDev e meanR
    # A partir disso, otimiza UM portfólio em cada universo, explicitando de cara
    # o retorno ou variância alvo dele
    # para isso, requeremos um pacote ou algo que ainda não temos

# Opção 6:
    # Solução analítica
